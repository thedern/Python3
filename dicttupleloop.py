d = {'a':10, 'b':20, 'c':30}

"""
multiple tuple assigment using a for loop and the dictionary's items() method.
a list of key/value tuples are created and then assigned to two variables and then
printed.

"""
#x = list(d.items())
print("Debug:", x)
for key, val in x:
    print(val, key)
